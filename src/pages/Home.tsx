import { Box, Container } from '@mui/material';

const Home = () => {
	return (
		<Box component={'section'}>
			<Container></Container>
			<h1>Головна сторінка</h1>
		</Box>
	);
};

export default Home;
